package com.group37.sentencebuilder.ui_layer;

/**
 * Identifiers for main shell views (UI navigation only).
 */
public enum ViewKey {
    HOME,
    IMPORT,
    GENERATE,
    AUTOCOMPLETE,
    REPORTS,
    CORPUS_STATS,
    SETTINGS
}
