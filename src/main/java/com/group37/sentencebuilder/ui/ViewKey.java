package com.group37.sentencebuilder.ui;

/**
 * Identifiers for main shell views (UI navigation only).
 */
public enum ViewKey {
    HOME,
    IMPORT,
    GENERATE,
    AUTOCOMPLETE,
    REPORTS,
    SETTINGS
}
